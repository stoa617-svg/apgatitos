# render.yaml - despliegue automático del backend en Render usando requirements.txt
services:
  - type: web
    name: kittycheck-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn kittycheck1.main:app --host 0.0.0.0 --port $PORT
